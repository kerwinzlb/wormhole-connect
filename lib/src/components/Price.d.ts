import { default as React } from 'react';

interface priceProps {
    children?: string;
    textAlign?: 'left' | 'right';
}
declare const Price: ({ children, textAlign }: priceProps) => React.JSX.Element;
export default Price;
//# sourceMappingURL=Price.d.ts.map